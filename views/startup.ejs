<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">

      <h1 class="text-center mb-3">
        <i class="fas fa-desktop"></i> System Startup
      </h1>

      <div class="card card-body"> 
        <h4 class="text-center">
          <i></i>Master Admin Register Form
        </h4>
        <%- include('./partials/messages.ejs') %>
        <form action="/admin/startup" method="POST">
          <div class="form-group">
            <label for="name">Name</label>
            <input
              type="name"
              id="name"
              name="name"
              class="form-control"
              placeholder="Enter Name"
              value="<%= typeof name != 'undefined' ? name : '' %>"
            />
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              class="form-control"
              placeholder="Create Password"
              value="<%= typeof password != 'undefined' ? password : '' %>"
            />
          </div>
          <div class="form-group">
            <label for="password2">Confirm Password</label>
            <input
              type="password"
              id="password2"
              name="password2"
              class="form-control"
              placeholder="Confirm Password"
              value="<%= typeof password2 != 'undefined' ? password2 : '' %>"
            />
          </div>
          <button type="submit" class="btn btn-primary btn-block">Startup</button>
        </form>
      </div>

      <div class="lead mt-4 text-center">
        <h6>
          <p>If VoterPass has alredy been Setup, you may go Register a new Admin to the System or Log into the Application.</p>
        </h6>
        <a class="btn btn-secondary btn-block" href="/admin/register">Register</a>  
        <a class="btn btn-secondary btn-block" href="/admin/login">Login</a>  
      </div>

    </div>
  </div>
</div>